# Backend base URL (no trailing slash). Copy to .env and adjust if needed.
REACT_APP_API_URL=http://localhost:5000

# Vercel / production: set in Project → Settings → Environment Variables to your API URL, e.g.
# REACT_APP_API_URL=https://education-hub-api.onrender.com
